{
    "mcpServers": {
        "shift-scheduler": {
            "command": "docker",
            "args": [
                "run",
                "-i",
                "--rm",
                "-e",
                "SHIFT_SCHEDULER_API_URL=http://host.docker.internal:8081",
                "shift-scheduler-mcp-server:latest"
            ]
        }
    }
}